{
  "name": "gdrive-file-summarizer",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && rollup --config rollup.config.mjs && cp appsscript.json dist/",
    "push": "npm run build && clasp push"
  },
  "devDependencies": {
    "@google/clasp": "3.3.0",
    "@rollup/plugin-typescript": "12.3.0",
    "@types/google-apps-script": "2.0.11",
    "rollup": "4.62.3",
    "tslib": "2.8.1",
    "typescript": "6.0.3"
  }
}
